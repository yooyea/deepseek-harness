/**
 * DeepHarness deployment-brand plugin, node half. The host entry is empty;
 * the browser half is discovered from the package's dsh.client manifest.
 */

/** Host plugin body — this branding overlay has no host-side behavior. */
export function apply(): void {}
